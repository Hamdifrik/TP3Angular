import { NompersoSkipTestsPipe } from './nomperso--skip-tests.pipe';

describe('NompersoSkipTestsPipe', () => {
  it('create an instance', () => {
    const pipe = new NompersoSkipTestsPipe();
    expect(pipe).toBeTruthy();
  });
});
